# Environment Configuration for Staging
ENVIRONMENT=staging
LOG_LEVEL=DEBUG

# Port (must be different from production)
APP_PORT=8001

# Database Configuration`n# Pour utiliser PostgreSQL, décommentez les lignes ci-dessous et commentez DB_PATH`n# USE_POSTGRESQL=true`n# DB_PATH=data_staging.db  # SQLite (fallback si PostgreSQL non disponible)

# CORS - Limit to staging URLs only
CORS_ORIGINS=http://localhost:8001,http://127.0.0.1:8001

# LLM Provider Configuration
LLM_PROVIDER=openai

# Rate limiting (more permissive in staging for testing)
RATE_LIMIT_REQUESTS=200
RATE_LIMIT_WINDOW=60

# PostgreSQL Configuration (décommentez pour activer)`n# USE_POSTGRESQL=true`n# POSTGRES_HOST=localhost`n# POSTGRES_PORT=5432`n# POSTGRES_DB=ovh_feedbacks_staging`n# POSTGRES_USER=ovh_tracker_staging`n# POSTGRES_PASSWORD=staging_password


# Environment Configuration for Staging
# Copy this file to .env.staging and fill in your values

# Environment
ENVIRONMENT=staging
LOG_LEVEL=DEBUG

# Port (must be different from production)
APP_PORT=8001

# Database SQLite (temporaire, sera migré vers PostgreSQL)
# Relative path from backend/ directory
DB_PATH=data_staging.db

# CORS - Limit to staging URLs only
CORS_ORIGINS=http://localhost:8001,http://127.0.0.1:8001

# LLM Provider Configuration
LLM_PROVIDER=openai
# OPENAI_API_KEY - Inherited from .env or set here
# ANTHROPIC_API_KEY - Optional, for Anthropic provider
# GOOGLE_API_KEY - Optional, for Google provider

# Rate limiting (more permissive in staging for testing)
RATE_LIMIT_REQUESTS=200
RATE_LIMIT_WINDOW=60

# PostgreSQL Configuration (for after migration)
# Uncomment and configure when ready to use PostgreSQL
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5433
# POSTGRES_DB=ovh_feedbacks_staging
# POSTGRES_USER=ovh_tracker_staging
# POSTGRES_PASSWORD=your_secure_staging_password_here

# Optional: Third-party API credentials
# GITHUB_TOKEN=your_github_token
# TRUSTPILOT_API_KEY=your_trustpilot_key
# LINKEDIN_CLIENT_ID=your_linkedin_client_id
# LINKEDIN_CLIENT_SECRET=your_linkedin_client_secret
# TWITTER_BEARER_TOKEN=your_twitter_bearer_token
